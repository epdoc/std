// This file is auto-generated by scripts/prebuild.ts. Do not edit directly.
import { rgb24 } from '@std/fmt/colors';
import { palette } from './colorspec.ts';

export const black = (s: string): string => rgb24(s, palette.black);
export const charcoal = (s: string): string => rgb24(s, palette.charcoal);
export const slate = (s: string): string => rgb24(s, palette.slate);
export const steel = (s: string): string => rgb24(s, palette.steel);
export const gray = (s: string): string => rgb24(s, palette.gray);
export const cream = (s: string): string => rgb24(s, palette.cream);
export const tan = (s: string): string => rgb24(s, palette.tan);
export const taupe = (s: string): string => rgb24(s, palette.taupe);
export const stone = (s: string): string => rgb24(s, palette.stone);
export const white = (s: string): string => rgb24(s, palette.white);
export const crimson = (s: string): string => rgb24(s, palette.crimson);
export const red = (s: string): string => rgb24(s, palette.red);
export const coral = (s: string): string => rgb24(s, palette.coral);
export const rose = (s: string): string => rgb24(s, palette.rose);
export const pink = (s: string): string => rgb24(s, palette.pink);
export const terracotta = (s: string): string => rgb24(s, palette.terracotta);
export const rust = (s: string): string => rgb24(s, palette.rust);
export const orange = (s: string): string => rgb24(s, palette.orange);
export const amber = (s: string): string => rgb24(s, palette.amber);
export const gold = (s: string): string => rgb24(s, palette.gold);
export const yellow = (s: string): string => rgb24(s, palette.yellow);
export const lemon = (s: string): string => rgb24(s, palette.lemon);
export const lime = (s: string): string => rgb24(s, palette.lime);
export const green = (s: string): string => rgb24(s, palette.green);
export const mint = (s: string): string => rgb24(s, palette.mint);
export const teal = (s: string): string => rgb24(s, palette.teal);
export const sage = (s: string): string => rgb24(s, palette.sage);
export const olive = (s: string): string => rgb24(s, palette.olive);
export const moss = (s: string): string => rgb24(s, palette.moss);
export const navy = (s: string): string => rgb24(s, palette.navy);
export const blue = (s: string): string => rgb24(s, palette.blue);
export const sky = (s: string): string => rgb24(s, palette.sky);
export const cyan = (s: string): string => rgb24(s, palette.cyan);
export const indigo = (s: string): string => rgb24(s, palette.indigo);
export const violet = (s: string): string => rgb24(s, palette.violet);
export const purple = (s: string): string => rgb24(s, palette.purple);
export const lavender = (s: string): string => rgb24(s, palette.lavender);
export const lilac = (s: string): string => rgb24(s, palette.lilac);
export const magenta = (s: string): string => rgb24(s, palette.magenta);
export const sand = (s: string): string => rgb24(s, palette.sand);
export const wheat = (s: string): string => rgb24(s, palette.wheat);
export const ochre = (s: string): string => rgb24(s, palette.ochre);
export const bronze = (s: string): string => rgb24(s, palette.bronze);
export const copper = (s: string): string => rgb24(s, palette.copper);
export const sienna = (s: string): string => rgb24(s, palette.sienna);
export const brown = (s: string): string => rgb24(s, palette.brown);
