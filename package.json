{
  "name": "epdoc-timeutil",
  "version": "2.1.2",
  "description": "Lightweight date and duration utilities, where moment.js does not have what we need",
  "license": "MIT",
  "author": {
    "name": "James Pravetz",
    "email": "jpravetz@epdoc.com"
  },
  "main": "dist/index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/jpravetz/epdoc-timeutil"
  },
  "scripts": {
    "clean": "rm -rf dist",
    "build": "tsc",
    "test": "jest --runInBand",
    "coverage": "jest --coverage --runInBand --verbose",
    "prepublishOnly": "tsc",
    "prepare": "npm run build",
    "format": "prettier --write 'src/**/*.ts'"
  },
  "dependencies": {
    "epdoc-util": "^0.1.17"
  },
  "devDependencies": {
    "@types/jest": "^29.5.5",
    "@types/node": "^20.7.1",
    "jest": "^29.7.0",
    "mocha": "^10.2.0",
    "prettier": "^2.8.8",
    "ts-jest": "^29.1.1",
    "tslint-config-prettier": "^1.18.0",
    "tslint-plugin-prettier": "^2.3.0",
    "typescript": "^5.2.2"
  }
}
